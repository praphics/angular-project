export class Team {
	constructor(public _links: string, public id: string, public code: string, public crestUrl: string, public name: string, public shortName: string, public squadMarketValue: string) {} 
}